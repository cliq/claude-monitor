.superpowers/
.DS_Store
build/
DerivedData/
*.xcuserstate
xcuserdata/

ClaudeMonitor.xcodeproj/
App/Info.plist
Widget/Info.plist
default.profraw

# Isolated git worktrees for branch work
.worktrees/

# Local signing overrides — each contributor keeps their own
Configuration/LocalSigning.xcconfig

# Exported signing cert / API key files — never commit
keys/

# ESP32 firmware — build output and per-device secrets
firmware/.pio/
firmware/include/secrets.h
