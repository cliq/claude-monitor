import SwiftUI

@main
struct ClaudeMonitorApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Claude Monitor — scaffolding")
                .frame(minWidth: 300, minHeight: 200)
        }
    }
}
